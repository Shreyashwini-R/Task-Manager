{
  "routes": [
    {
      "route": "/api/*",
      "methods": ["GET", "POST", "DELETE", "OPTIONS"],
      "allowedRoles": ["anonymous"]
    }
  ],
  "navigationFallback": {
    "rewrite": "/index.html"
  }
}